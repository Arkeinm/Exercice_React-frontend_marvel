import "./pagination.css";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

function Pagination({ limit, actualPage, numberOfPages }) {
  return <div className="pagination"></div>;
}

export default Pagination;
